yamlApplicationConfig:
  kafka:
    clusters:
      - name: yaml
        bootstrapServers: my-cluster-kafka-bootstrap.kafka.svc:9092
        schemaRegistry: "http://apicurio-registry.kafka.svc:8080/apis/ccompat/v7"

  auth:
    type: disabled
  management:
    health:
      ldap:
        enabled: false